# Worker API URL (deploy your worker first, then paste the URL here)
NEXT_PUBLIC_WORKER_URL=http://localhost:8787

# Admin dashboard password (must match ADMIN_PASSWORD in worker/wrangler.toml)
NEXT_PUBLIC_ADMIN_PASSWORD=changeme
